

/*
export interface Set {
    nombre:  string;
    partes:  Parte[];
    bonuses: Bonus[];
}

export interface Bonus {
    nombre:     string;
    listaBonus: ListaBonus[];
}

export interface ListaBonus {
    nombreAtributo: string;
    valor:          number;
    action:         null;
    impact:         null;
    condition:      null;
    time:           null;
}

export enum BonusNombre {
    TotalBonusCombo = "total bonus combo",
}

export interface Parte {
    nombre:   string;
    atributo: Atributo;
    valor:    number;
}

export interface Atributo {
    nombre: string;
}

export enum AtributoNombre {
    PhysicalAttack = "physical attack",
    PhysicalDefense = "physical defense",
    Power = "power",
    Speed = "speed",
    StrategyDefense = "strategy defense",
}
*/